{
  "name": "ROOT Physics Environment",
  "image": "rootproject/root:latest",

  "postCreateCommand": "apt-get update && apt-get install -y python3-pip && pip3 install ipykernel jupyterlab numpy scipy matplotlib",

  "forwardPorts": [8888],

  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-toolsai.jupyter"
      ],
      "settings": {
        "python.defaultInterpreterPath": "/usr/bin/python3"
      }
    }
  }
}
